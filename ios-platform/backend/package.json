{
    "name": "ios-platform/backend",
    "private": true,
    "type": "project",
    "scripts": {
        "dev": "php artisan serve --host=0.0.0.0 --port=8000",
        "test": "php artisan test",
        "lint": "pint --preset psr12 app/",
        "format": "pint app/"
    }
}
